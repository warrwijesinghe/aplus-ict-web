# Public image map

Public-facing image IDs are supplied by the consolidated API's resource records. Upload and resource metadata are managed through Admin/API; local storage keys are private implementation details.

Do not run retired standalone-resource scripts. Any existing image inventory should be registered through the modular API after the resource migration is verified.
