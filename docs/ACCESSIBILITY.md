# Accessibility foundations

Layouts use semantic landmarks, a skip link, visible keyboard focus, labelled controls, `aria-invalid` form fields, error alerts, and loading announcements. Responsive grids avoid fixed-width layouts, portal navigation collapses on narrow screens, and reduced-motion preferences disable transitions.
