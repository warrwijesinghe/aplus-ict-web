# API contract

Web uses exactly one environment value: `VITE_API_URL`. All client modules share that base and call the versioned modular-monolith API under `/api/v1`.

The previous service-by-service contract audit is retired historical documentation. It must not be used to configure development or deployment.
